name: 🔁 6th lvl reusable workflow
on:
  workflow_call: {}

jobs:
  fourth-level-reusable-wf-job:
    name: 🔁 6th lvl reusable workflow job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: run script
        run: |
          echo "Hello, world!"

