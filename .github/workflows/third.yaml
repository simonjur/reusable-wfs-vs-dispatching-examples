name: 3rd lvl reusable workflow
on:
  workflow_call: {}

jobs:
  third-level-reusable-wf-job:
    name: Third level reusable workflow job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: run script
        run: |
          echo "Hello, world!"

#  second-level-reusable-wf:
#    name: Second level reusable workflow
#    runs-on: ubuntu-latest
#    uses: ./.github/workflows/second.yaml
